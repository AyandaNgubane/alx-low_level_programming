#include "main.h"

/**
 * main - Write a function that adds two integers and returns the result.
 *
 * Return: 0 if successful
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
